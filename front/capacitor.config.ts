import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mathis.miellerie',
  appName: 'Miellerie',
  webDir: 'dist/front/browser',
};

export default config;
